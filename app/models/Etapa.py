from Matrix import Matrix
class Etapa:
    def __init__(self, rango, nro) -> None:
        self.matrix = Matrix()
        self.matrix.set_Size_of_Matrix(nro, rango)
    def iterations(self, r, f):



















